diff --git a/firmware/src/main.cpp b/firmware/src/main.cpp
index d40574e..7a7eb7e 100644
--- a/firmware/src/main.cpp
+++ b/firmware/src/main.cpp
@@ -362,17 +362,37 @@ void setup()
   init_mic_spk();
 
   /// settings
+  // Config sources:
+  // - AtomS3R: SPIFFS only (no SD)
+  // - Core2 / CoreS3: prefer SD (upstream layout), fall back to SPIFFS so
+  //   boards without a microSD still work. SPIFFS layout matches AtomS3R:
+  //   /SC_ExConfig.yaml, /SC_SecConfig.yaml, /SC_BasicConfig.yaml
+  bool config_loaded = false;
 #if defined(ARDUINO_M5STACK_ATOMS3R)
   if (SPIFFS.begin()) {
-    // この関数ですべてのYAMLファイル(Basic, Secret, Extend)を読み込む
     system_config.loadConfig(SPIFFS, "/SC_ExConfig.yaml", 2048,
                                      "/SC_SecConfig.yaml", 2048,
                                      "/SC_BasicConfig.yaml", 2048);
+    config_loaded = true;
+    Serial.println("Config loaded from SPIFFS (AtomS3R).");
+  }
 #else
   if (SD.begin(GPIO_NUM_4, SPI, 25000000)) {
-    // この関数ですべてのYAMLファイル(Basic, Secret, Extend)を読み込む
+    // Upstream paths: /app/AiStackChanEx/SC_ExConfig.yaml + /yaml/*.yaml
     system_config.loadConfig(SD, "/app/AiStackChanEx/SC_ExConfig.yaml");
+    config_loaded = true;
+    Serial.println("Config loaded from SD card.");
+  } else if (SPIFFS.begin(true)) {
+    system_config.loadConfig(SPIFFS, "/SC_ExConfig.yaml", 2048,
+                                     "/SC_SecConfig.yaml", 2048,
+                                     "/SC_BasicConfig.yaml", 2048);
+    config_loaded = true;
+    Serial.println("SD not found; config loaded from SPIFFS.");
+    M5.Lcd.println("Config: SPIFFS (no SD)");
+  }
 #endif
+
+  if (config_loaded) {
     // Wifi設定読み込み
     wifi_s* wifi_info = system_config.getWiFiSetting();
     Serial.printf("\nSSID: %s\n",wifi_info->ssid.c_str());
@@ -387,10 +407,10 @@ void setup()
     if(Wifi_connection_check()){
       Serial.println("Successfully connected to Wi-Fi using the previous settings.");
     }else{
-      // 前回設定での接続に失敗。SDカード設定による接続にトライ。
-      Serial.println("The previous WiFi connection failed. Attempting to connect using the SD card settings.");
+      // 前回設定での接続に失敗。YAML設定による接続にトライ。
+      Serial.println("The previous WiFi connection failed. Attempting to connect using YAML settings.");
       if(wifi_info->ssid.length() == 0){
-        // SDカード設定の取得に失敗。Smart Configをスタート。
+        // YAML設定の取得に失敗。Smart Configをスタート。
         Serial.println("Can't get WiFi settings. Start Smart Config.");
         if(!WifiSmartConfig()){
           // Smart Config失敗。オフラインモード。
@@ -400,13 +420,10 @@ void setup()
       }else{
         WiFi.begin(wifi_info->ssid.c_str(), wifi_info->password.c_str());
         if(Wifi_connection_check()){
-          // SDカード設定による接続に成功。
-          Serial.println("Successfully established a Wi-Fi connection via the SD card settings.");
+          Serial.println("Successfully established a Wi-Fi connection via YAML settings.");
         }else{
-          // SDカード設定による接続に失敗。Smart Configをスタート。
-          Serial.println("WiFi connection failed due to SD card settings. Start Smart Config.");
+          Serial.println("WiFi connection failed due to YAML settings. Start Smart Config.");
           if(!WifiSmartConfig()){
-            // Smart Config失敗。オフラインモード。
             Serial.println("Smart Config failed. Running in offline mode.");
             isOffline = true;
           }
@@ -436,10 +453,9 @@ void setup()
 
     //SD.end();
   } else {
-    M5.Lcd.print("Failed to load SD card settings. System reset after 5 seconds.");
+    M5.Lcd.print("Failed to load settings (no SD / SPIFFS). Reset in 5s.");
     delay(5000);
     ESP.restart();
-    //WiFi.begin();
   }
   
   mp3_init();
